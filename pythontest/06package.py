# 使用
# 1.导入包
# 有两种带入方式：
# 第一种是import 包名
import time
import math
import random
time.sleep(10) # 导入后，可以调用一个time包自带的方法sleep
print("你好")  # 此时输出的结果会等10s后才打印出来
print(math.sqrt(4)) #此时输出的结果为4的平方根
print(random.randint(1,10)) #产生1-10的随机数
